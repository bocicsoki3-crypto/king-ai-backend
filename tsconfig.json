{
  "compilerOptions": {
    /* Alapvető beállítások (Ezek helyesek voltak) */
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "strict": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "noImplicitAny": true, // Megtartva az eredeti konfigból

    /* === Architektúra Javítás === */
    // A forrásfájlok a gyökérben kezdődnek
    "rootDir": "./", 
    // Minden lefordított .js fájl ide kerül
    "outDir": "./dist", 
    
    "declaration": false,
    "allowJs": true
  },
  
  /* === A TS2304 HIBA VÉGLEGES JAVÍTÁSA ===
   * Ez garantálja, hogy a tsc megtalálja az ÖSSZES .ts fájlt
   * és (kritikusan) az összes típusdefiníciót (pl. ICanonicalRichContext)
   * a fordítás megkezdése előtt.
   */
  "include": [
    "**/*.ts",           // Minden .ts fájl a projektben
    "src/types/**/*.d.ts" // Garantálja, hogy a típusok ismertek
  ],
  
  "exclude": [
    "node_modules",
    "dist", // Ne olvassa be a saját kimenetét
    "script.ts" // A kliens szkript kizárása
  ]
}
